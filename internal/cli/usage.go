package cli

var cmdUsage = `usage: gitmirror [options]

Use git commands to clone and update backups of git repositories.

Options
        --config=FILE   Use FILE as config file (default ~/.gitmirror.json)
        --quiet         Print only error messages

    -h, --help          Print this help and exit
        --version       Print version and exit

For more information or to file an issue, visit https://github.com/telemachus/gitmirror`
